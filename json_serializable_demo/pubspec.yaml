name: json_serializable_demo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  # For using annotations with json_serializable
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # For code generation
  build_runner: ^1.10.0

  # Highlighted package
  json_serializable: ^3.3.0

flutter:
  uses-material-design: true
