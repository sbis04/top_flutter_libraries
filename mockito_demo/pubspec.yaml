name: mockito_demo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  # For HTTP connection
  http: ^0.13.3

  # For UI template
  velocity_x: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Highlighted package
  mockito: ^5.0.8

  # For code generation
  build_runner: ^2.0.3

flutter:
  uses-material-design: true
